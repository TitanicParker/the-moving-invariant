# Original flat upload

The repository was initially uploaded as a transport package in which directory separators were encoded as `__` in filenames. That state is preserved in Git history at commit:

`cb2b615ce07365a9880027f83fa3d1311509ce81`

The repair branch reconstructs the intended executable GitHub/Astro tree rather than carrying the flat transport package forward as the live repository.