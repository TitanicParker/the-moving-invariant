# Contributing

Contributions should add evidence, protocols, replications, failures, or explicit primitive challenges without silently rewriting source history. New experiments need unique IDs, evidence labels, machine-readable records and provenance. Run `npm run validate` and `npm run build` before proposing changes.
