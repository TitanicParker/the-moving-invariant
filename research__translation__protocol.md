# Translation invariance protocol

Freeze source → resolve source → translate independently → hide source coordinate from target resolver → resolve target → freeze target → compare → preserve disagreement.

No multilingual result is seeded in the foundation build.
