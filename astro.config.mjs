import { defineConfig } from 'astro/config';
export default defineConfig({
  site: 'https://TitanicParker.github.io',
  base: '/the-moving-invariant',
  output: 'static',
  trailingSlash: 'never'
});
